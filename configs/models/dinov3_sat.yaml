# H5 foundation arm: DINOv3 ViT-L/16 SAT-493M frozen backbone + trained head (variant: finetuned).
# GATED: needs cuda + HF_TOKEN (license accepted); otherwise the arm skip-and-logs (5.4).
model: {name: dinov3_sat, backbone: null, results_backbone: vitl16-sat493m, pretrained: true, revision: f692fa42da72c6797b67cd73494a168d1120d3ee}
train: {batch_size: 8, max_epochs: 50, lr: 3e-4, weight_decay: 1e-4, dice_weight: 1.0,
        ignore_index: 255, early_stop_patience: 10, grad_clip: 1.0}
