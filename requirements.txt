# requirements.txt (CORE — Windows/CPU clean install) for the Mars terrain-segmentation project.
# Everything here installs on native Windows/CPU and powers the full pipeline EXCEPT heavy GPU
# training (done on the V100) and the SAM/DINOv3 foundation models (requirements-extras.txt).

# --- numerical / data core ---
numpy>=1.26,<2.1
pandas>=2.1,<2.3
pyarrow>=15,<18
scipy>=1.11,<2
scikit-learn>=1.4,<2
scikit-image>=0.22,<1     # boundary-F1, morphology, label IO
pyyaml>=6.0,<7
python-dotenv>=1.0,<2
tqdm>=4.66,<5
tabulate>=0.9,<1
requests>=2.31,<3         # dataset download

# --- plotting ---
matplotlib>=3.8,<4
seaborn>=0.13,<1

# --- image IO + augmentation ---
pillow>=10.0,<13
opencv-python-headless>=4.9,<5
albumentations>=1.4,<3

# --- deep learning (CV) ---
torch>=2.2,<2.5
torchvision>=0.17,<0.20
segmentation-models-pytorch>=0.3.3,<1  # U-Net / DeepLabV3+ + ImageNet-pretrained encoders
transformers>=4.40,<6                # SegFormer (MiT) segmentation
lightning>=2.2,<2.6                  # training orchestration (loop, AMP, DDP, checkpoint, early-stop)
torchmetrics>=1.3,<2                 # mIoU / per-class IoU (MulticlassJaccardIndex, ignore_index)

# --- tooling ---
ruff>=0.5,<1
black>=24.4,<27
pytest>=8.0,<10
pre-commit>=3.8,<5
