# requirements-extras.txt — foundation segmentation models (H5). Best run on the V100 GPU profile;
# the pipeline skip-and-logs these if absent.
#
# DINOv3 (the primary H5 foundation arm) loads via `transformers` (already in core requirements) as
# `facebook/dinov3-vitl16-pretrain-sat493m` — the ViT-L variant pretrained on Earth satellite
# imagery (SAT-493M). The weights are gated: accept their license on Hugging Face and set
# HF_TOKEN in .env. No extra pip install is needed for DINOv3 itself.
segment-anything>=1.0,<2  # Meta SAM region proposals; needs a separately downloaded checkpoint
timm>=1.0,<2              # ViT backbones / alternate DINOv3 loading path
